declare class Root extends NodeBase {
    constructor();
    insertContained(tree: any, itemEnv: any, item: any): void;
    isSearchMatch(searchEnv: any): boolean;
    insert(itemEnv: any, item: any): any;
}
declare namespace Root {
    let origin: Coordinate;
}
export default Root;
import NodeBase from './NodeBase.js';
import Coordinate from '../../geom/Coordinate.js';
