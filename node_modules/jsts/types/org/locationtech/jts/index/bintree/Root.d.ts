declare class Root extends NodeBase {
    constructor();
    insertContained(tree: any, itemInterval: any, item: any): void;
    isSearchMatch(interval: any): boolean;
    insert(itemInterval: any, item: any): any;
}
declare namespace Root {
    let origin: number;
}
export default Root;
import NodeBase from './NodeBase.js';
