HandlingStrokesAndFills

Started: 2026-03-29

`/initialize` retires `current` and starts a new dated folder; numbered artifacts `1_`–`4_` and (from `/document`) `0_Overview.md` go in this folder via **`.cursor/features/current/`**.
